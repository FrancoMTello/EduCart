services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: educart_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:////app/educart.db
      - SECRET_KEY=educart_super_secret_key_2026
    restart: always